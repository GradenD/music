<?php 
    global $auth; 
    if($auth){
        global $usrId;
        global $usrName;
        global $usrLast;
        global $usrPhone;
        global $usrMail;
        global $usrSkype;
        global $usrInstagram;
        global $usrCountry;
    }
?>